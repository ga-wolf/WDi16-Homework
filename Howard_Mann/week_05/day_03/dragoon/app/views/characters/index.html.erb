<h2>Heroes</h2>

<div class="index-container">

  <% @characters.each do |el| %>
    <div class="race-float">
      <p><strong><%= el.name %></strong></p>
      <%= link_to(image_tag( el.image ), character_path(el.name)) %>
    </div>
  <% end %>

</div>
