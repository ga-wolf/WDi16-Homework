<h2>Races</h2>

<div class="index-container">

  <% @races.each do |el| %>
    <div class="race-float">
      <p><strong><%= el.name %></strong></p>
      <%= link_to(image_tag(el.image),race_path(el.name)) %>
    </div>
  <% end %>

</div>
